// Mapping: normalizeKey(text) -> Array von Bildpfaden (aus /public)
// Beispiel-Key: "casa azul frida kahlo museum" -> ["/bilder/casa-azul-1.jpg", "/bilder/casa-azul-2.jpg"]
export const SUBPOINT_IMAGES = {
  // TODO: Hier eure Bilder eintragen
};
